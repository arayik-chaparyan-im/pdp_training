Task_1. Write bash command(s) to show count of all Google Chrome processes.

ps aux | grep chrome | wc -l

Task_2. Write bash command(s) to read content of "test.txt" file, change all "test" keywords with "DONE" keyword and write in "test.result.txt" file.

we use

cat test.txt

to read content of "test.txt" file

after we write: 

sed 's/test/DONE/g' test.txt > test.result.txt

where:

sed 's/{old word}/{new word}/g' {name_of_old_file} > {name_of_new-file}
